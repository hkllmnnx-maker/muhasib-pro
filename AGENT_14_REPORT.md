# تقرير المهمة — Agent 14: لوحة التقدّم ومسار المحاسب اليمني والشهادات

**الفرع:** `agent-14-yemen-progress-certificates`
**التاريخ:** 2026-06-01
**الحالة:** ✅ مكتملة — البناء ناجح (`npm run build`)

---

## 1. ملخّص المهمة

تحسين تجربة تتبّع تقدّم المتعلّم على منصّة "محاسب برو"، وإضافة مسار تعليمي
باسم **"مسار المحاسب اليمني"** يربط الدورات الموجودة، مع شهادة إتمام تعليمية
قابلة للطباعة. تم العمل بالكامل مع التركيز على تجربة المستخدم والتقدّم **دون
إضافة أي محتوى قانوني أو ضريبي جديد**.

---

## 2. ما تم إنجازه

### أ) لوحة تقدّم المتعلّم (صفحة `/roadmap`)
- **نسبة تقدّم عامة** معروضة كحلقة دائرية متحركة (SVG) بنسبة مئوية محسوبة من
  إجمالي دروس المنصة.
- **عدّاد الدروس المكتملة** + إجمالي الدروس + عدد الدورات التي بدأها المتعلّم +
  عدد الدورات المكتملة.
- **اقتراح الدرس التالي** ("تابع من حيث توقّفت"): يحدّد أول درس غير مكتمل بالترتيب
  ويعرض عنوانه واسم دورته ورابطًا مباشرًا له. وعند عدم بدء أي درس يظهر صندوق "ابدأ الآن".
- أشرطة تقدّم لكل دورة ضمن "المسار الكامل للمنصة".

### ب) مسار المحاسب اليمني (قسم في `/roadmap`)
يربط الدورات **الموجودة فقط** بترتيب عملي:
1. أساسيات المحاسبة — `fundamentals`
2. القوائم المالية — `financial-statements`
3. الضرائب اليمنية — `taxation-vat`
4. المحاسبة الحكومية اليمنية (اختيارية، تُعرض عند توفّرها) — `governmental-accounting`
5. إكسل للمحاسبين — `excel-for-accountants`

- شريط تقدّم للمسار ككل + شريط تقدّم لكل خطوة.
- تنويه واضح بأن المسار "يربط دورات المنصة الموجودة ولا يضيف معلومات قانونية أو ضريبية جديدة".
- آلية حماية ذكية: أي خطوة تشير إلى دورة غير موجودة تُحذف تلقائيًا (لا اختلاق).

### ج) شهادة الإتمام التعليمية (صفحة `/certificate/:slug`)
- **قابلة للطباعة** عبر زر طباعة/حفظ PDF مع أنماط `@media print` (A4 أفقي).
- لا تظهر إلا بعد إكمال **جميع** دروس المسار (بوابة إكمال client-side).
- إدخال اسم المتعلّم + تاريخ إصدار + رقم شهادة فريد محفوظ محليًا.
- **تنويه إلزامي**: مكتوب صراحةً أنها "شهادة إتمام تعليمية" من المنصة، و**ليست
  اعتمادًا مهنيًا رسميًا** ولا ترخيصًا ولا بديلًا عن الجهات التنظيمية المعتمدة.

---

## 3. الملفات المتأثّرة

| الملف | النوع | الوصف |
|------|------|------|
| `src/data/learning-paths.ts` | جديد | تعريف مسار المحاسب اليمني ودوال ربط الدورات الموجودة |
| `src/pages/roadmap.tsx` | تعديل | لوحة تقدّم تفاعلية + قسم المسار اليمني + أشرطة تقدّم |
| `src/pages/certificate.tsx` | جديد | صفحة شهادة الإتمام التعليمية القابلة للطباعة |
| `src/index.tsx` | تعديل | إضافة مسار `/certificate/:slug` |
| `public/static/app.js` | تعديل | منطق لوحة التقدّم + الدرس التالي + المسار اليمني + الشهادة |
| `public/static/style.css` | تعديل | تنسيقات اللوحة والمسار والشهادة + أنماط الطباعة |

> ملاحظة: التقدّم يُحفظ محليًا في متصفّح المستخدم (`localStorage`) عبر نظام
> `MuhasibProgress` الموجود مسبقًا — لم يُضف أي تخزين خادمي.

---

## 4. الالتزام بالشروط

- ✅ **فرع مستقل**: `agent-14-yemen-progress-certificates`.
- ✅ **`npm run build` ناجح** (يُبنى `dist/_worker.js`).
- ✅ **لا محتوى قانوني/ضريبي جديد**: المسار يربط الدورات الموجودة فقط، والنصوص
  المعروضة تصف دور كل خطوة دون اختلاق أرقام أو أحكام قانونية/ضريبية.
- ✅ **الشهادة تعليمية وليست اعتمادًا مهنيًا** (تنويه مكتوب على الشهادة نفسها).
- ✅ **رفع تدريجي**: تم رفع كل مرحلة فور إنجازها (المسار → اللوحة → الشهادة).

---

## 5. المصادر المعرفية

**لم يُضَف أي محتوى معرفي/قانوني/ضريبي جديد في هذا الفرع** التزامًا بالشرط
الإلزامي للمهمة. كل المعلومات المحاسبية والضريبية تبقى ضمن الدورات الأصلية كما
هي، ولم يُعدَّل عليها. لذلك لا توجد مصادر خارجية جديدة لإدراجها.

العمل اقتصر على:
- بنية تجربة المستخدم (تتبّع التقدّم، اقتراح الدرس التالي).
- ربط الدورات الموجودة في مسار منظّم.
- توليد شهادة إتمام تعليمية من بيانات المنصة نفسها.

---

## 6. خطوات لاحقة مقترحة

- ربط المسار اليمني بدورات إضافية حال إضافتها (مثل المحاسبة الحكومية الموسّعة)
  عبر إضافة خطوة جديدة في `learning-paths.ts` فقط.
- إمكانية إضافة مسارات تخصصية أخرى لاحقًا باستخدام نفس البنية (`LearningPath`).
- (اختياري ومستقبلي) إذا طُلب لاحقًا محتوى ضريبي يمني محدّث، يجب البحث من مصادر
  يمنية موثوقة قبل أي إضافة، في فرع منفصل ومخصّص لذلك.
