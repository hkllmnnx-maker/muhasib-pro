# دليل الوكلاء — مشروع "محاسب برو" (منصة تعلّم المحاسبة)

> اقرأ هذا الملف بالكامل قبل أن تبدأ. هو المرجع الوحيد للأنماط والمخططات.

## نظرة عامة
منصة عربية لتعليم المحاسبة المالية مبنية بـ **Hono + JSX + Vite + Cloudflare Pages**.
كل المحتوى عربي RTL. البيانات منظّمة في ملفات منفصلة داخل `src/data/`.

## القاعدة الذهبية لعملك
1. **أنشئ ملفات جديدة فقط.** لا تعدّل `src/data/index.ts` ولا `src/data/quizzes.ts` ولا `articles.ts`/`glossary.ts` إلا إذا كانت مهمتك تنصّ على ذلك صراحة.
2. **لا تغيّر أي ملف موجود** خارج نطاق مهمتك (تجنّب التعارضات).
3. اعمل على **فرعك المحدد فقط**. لا تدمج، لا تلمس `main`.
4. **ارفع عملك مباشرة بعد إنهائه** (commit + push) ولا تؤجّل.
5. اكتب محتوى **غزيراً ومفيداً ودقيقاً علمياً** بالعربية الفصحى.

## مخطط الأنواع (types.ts) — التزم به حرفياً

### الدورة Course
```ts
{
  id: 'c16',                    // معرّف فريد cXX
  slug: 'unique-english-slug',  // فريد بالإنجليزية بشرطات
  title: 'العنوان الكامل',
  shortTitle: 'عنوان مختصر',
  description: 'وصف قصير سطرين',
  longDescription: 'وصف مطوّل فقرة كاملة',
  level: 'beginner' | 'intermediate' | 'advanced' | 'professional',
  icon: 'fa-XXX',               // أيقونة fontawesome 6 (مثل fa-coins)
  color: 'linear-gradient(135deg, #HEX 0%, #HEX 100%)',
  duration: '18 ساعة',
  rating: 4.7,                  // رقم بين 4.5 و 4.9
  studentsCount: 3500,          // رقم تقريبي
  prerequisites: ['متطلب 1', 'متطلب 2'],
  objectives: ['هدف 1', 'هدف 2', ...],   // 5-6 أهداف
  tags: ['وسم1', 'وسم2', ...],
  modules: [ /* وحدات */ ]
}
```

### الوحدة Module والدرس Lesson
```ts
modules: [
  {
    id: 'm1',
    title: 'عنوان الوحدة',
    description: 'وصف الوحدة',
    lessons: [
      {
        id: 'l102',              // رقم درس فريد من النطاق المخصص لك
        slug: 'lesson-slug',     // فريد بالإنجليزية
        title: 'عنوان الدرس',
        summary: 'ملخص سطر واحد',
        duration: 25,            // بالدقائق (رقم)
        quizId: 'quiz-xxx',      // اختياري — يجب أن يطابق اختباراً أنشأته
        content: [ /* كتل المحتوى */ ]
      }
    ]
  }
]
```

### كتل المحتوى ContentBlock — الأنواع المتاحة
```ts
// 1. عنوان فرعي
{ type: 'heading', text: 'العنوان' }
{ type: 'subheading', text: 'عنوان أصغر' }

// 2. فقرة (يدعم HTML مثل <strong>)
{ type: 'paragraph', text: 'نص الفقرة ...' }

// 3. قائمة نقطية / مرقمة (يدعم HTML داخل العناصر)
{ type: 'list', items: ['عنصر', '<strong>عنوان:</strong> شرح'] }
{ type: 'ordered', items: ['خطوة 1', 'خطوة 2'] }

// 4. جدول
{ type: 'table', headers: ['عمود1', 'عمود2'], rows: [['خلية','خلية'], ['خلية','خلية']] }

// 5. صندوق تنبيه — variant: info | tip | warning | example
{ type: 'callout', variant: 'tip', title: 'العنوان', text: 'المحتوى' }

// 6. معادلة — استخدم الحقل text (وليس latex)
{ type: 'formula', title: 'اسم المعادلة', text: 'الربح = الإيراد − المصروف' }

// 7. مثال محلول
{ type: 'example', title: 'مثال', text: 'نص المثال ...' }

// 8. قيد محاسبي — استخدم الحقل lines (مهم جداً وليس entries)
{ type: 'journal', title: 'قيد كذا', lines: [
  { account: 'من ح/ الصندوق', debit: 1000 },
  { account: 'إلى ح/ رأس المال', credit: 1000 },
]}
```

⚠️ **تحذيران حرجان:**
- القيد المحاسبي `journal` يستخدم الحقل **`lines`** وليس `entries`.
- المعادلة `formula` تستخدم الحقل **`text`** وليس `latex`.

### الاختبار Quiz (في ملف اختبارات منفصل خاص بك)
```ts
import type { Quiz } from './types'

export const quizzesXxx: Quiz[] = [
  {
    id: 'quiz-xxx',                    // فريد، يطابق quizId في الدرس
    title: 'عنوان الاختبار',
    description: 'وصف قصير',
    courseSlug: 'نفس-slug-الدورة',
    questions: [
      {
        id: 'q1',
        question: 'نص السؤال؟',
        options: ['خيار أ', 'خيار ب', 'خيار ج', 'خيار د'],
        correctIndex: 1,               // فهرس الإجابة الصحيحة (يبدأ من 0)
        explanation: 'شرح لماذا هذه الإجابة صحيحة.',
      },
      // 3 أسئلة على الأقل لكل اختبار
    ],
  },
]
```

## مثال كامل لملف دورة (انسخ هذا النمط)
انظر إلى الملفات الموجودة `src/data/course-banking.ts` أو `src/data/course-corporate.ts` كأمثلة مرجعية كاملة.

## معايير الجودة المطلوبة
- كل دورة: **3-4 وحدات**، كل وحدة **2-3 دروس**، إجمالي **6-8 دروس** للدورة.
- كل درس: **محتوى غني** (فقرات + عناوين + قوائم + جداول + أمثلة + قيود محاسبية + صناديق تنبيه + معادلات). لا تقل عن 6 كتل محتوى لكل درس، والأفضل 8-12.
- كل درس مهم له اختبار (quizId) من 3 أسئلة على الأقل.
- المحتوى دقيق علمياً ومتوافق مع المعايير المحاسبية الدولية والعربية.
- العربية فصيحة واضحة، والمصطلحات الإنجليزية بين قوسين عند أول ذكر.

## خطوات العمل (نفّذها بالترتيب)
1. استنسخ المستودع وانتقل لفرعك (التعليمات في برومبتك).
2. اقرأ `AGENT_GUIDE.md` و`src/data/types.ts` وملف دورة موجود كمثال.
3. أنشئ ملفاتك الجديدة فقط.
4. لا تبنِ المشروع كاملاً إن كان بطيئاً؛ تحقق من صحة TypeScript لملفك فقط إن أمكن.
5. commit ثم push إلى فرعك مباشرة.
